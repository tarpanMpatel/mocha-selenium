#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mocha-runner/bin/runner.js" "$@"
  ret=$?
else 
  node  "$basedir/../mocha-runner/bin/runner.js" "$@"
  ret=$?
fi
exit $ret
